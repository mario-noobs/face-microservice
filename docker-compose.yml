services:
  # ==================== Infrastructure ====================
  mysql:
    image: mysql:8.0
    container_name: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root_password_secret_tcp
      - MYSQL_DATABASE=backend_db
    volumes:
      - mysql:/var/lib/mysql
    networks:
      - face
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  redis:
    image: redis:7.4-alpine
    container_name: redis
    volumes:
      - redis:/data
    networks:
      - face
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      timeout: 5s
      retries: 5

  minio:
    image: quay.io/minio/minio
    container_name: minio
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: "123456789"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - face
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  minio-init:
    image: minio/mc:latest
    container_name: minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - face
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 admin 123456789;
      mc mb --ignore-existing myminio/face-models;
      mc mb --ignore-existing myminio/face-images;
      echo 'MinIO buckets initialized';
      "
    restart: "no"

  vault:
    image: hashicorp/vault:1.15
    container_name: vault
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: dev-root-token
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
      VAULT_ADDR: http://127.0.0.1:8200
    ports:
      - "8200:8200"
    cap_add:
      - IPC_LOCK
    networks:
      - face
    healthcheck:
      test: ["CMD", "vault", "status"]
      interval: 10s
      timeout: 5s
      retries: 5

  vault-init:
    image: hashicorp/vault:1.15
    container_name: vault-init
    environment:
      VAULT_ADDR: http://vault:8200
      VAULT_TOKEN: dev-root-token
    volumes:
      - ./vault:/vault
    networks:
      - face
    depends_on:
      vault:
        condition: service_healthy
    entrypoint: ["/bin/sh", "/vault/dev-init.sh"]
    restart: "no"

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - face
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5

  # ==================== Backend (Java Modular Monolith) ====================
  backend-service:
    build:
      context: ./backend-service
      dockerfile: Dockerfile
    container_name: backend-service
    ports:
      - "8080:8080"
    environment:
      # Server
      - SERVER_PORT=8080
      # Database
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_NAME=backend_db
      - DB_USERNAME=root
      - DB_PASSWORD=root_password_secret_tcp
      # Redis
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      # JWT (secret must be base64 encoded, expiration in milliseconds)
      - JWT_SECRET=dmVyeS1pbXBvcnRhbnQtcGxlYXNlLWNoYW5nZS1pdC1pbi1wcm9kdWN0aW9uLXdpdGgtYXQtbGVhc3QtMjU2LWJpdHM=
      - JWT_ACCESS_TOKEN_EXPIRATION=3600000
      - JWT_REFRESH_TOKEN_EXPIRATION=604800000
      # MinIO
      - MINIO_ENDPOINT=http://minio:9000
      - MINIO_ACCESS_KEY=admin
      - MINIO_SECRET_KEY=123456789
      - MINIO_BUCKET=face-images
      # Face Recognition Service
      - FACE_SERVICE_URL=http://face-recognition-service:5000
      - SPRING_PROFILES_ACTIVE=dev
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      # RabbitMQ
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USERNAME=guest
      - RABBITMQ_PASSWORD=guest
      # Mail (Gmail SMTP)
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=587
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - EMAIL_FROM=${MAIL_USERNAME}
      # Elasticsearch
      - ELASTICSEARCH_URIS=http://elasticsearch:9200
      # Alerts
      - ALERT_RECIPIENTS=${ALERT_RECIPIENTS}

    networks:
      - face
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      vault-init:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
      elasticsearch:
        condition: service_started
    restart: on-failure
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # ==================== AI Service (Python - Stateless) ====================
  face-recognition-service:
    build: ./face-ai-service
    container_name: face-recognition-service
    networks:
      - face
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - MODEL_S3_ENDPOINT=http://minio:9000
      - MODEL_S3_ACCESS_KEY=admin
      - MODEL_S3_SECRET_KEY=123456789
      - MODEL_S3_BUCKET=face-models
      - MODEL_CACHE_DIR=/app/model_cache
      - ENABLED_MODELS=retinaface_mobilenet,facenet_mobilenet
      - LOG_LEVEL=INFO
    volumes:
      - face-model-cache:/app/model_cache
    depends_on:
      minio:
        condition: service_healthy
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s

  # ==================== Frontend ====================
  app-fe:
    build: ./gui-app
    container_name: app-fe
    ports:
      - "80:80"
    networks:
      - face
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    environment:
      - VITE_APP_API_URL=/api
    depends_on:
      backend-service:
        condition: service_healthy
    restart: on-failure

  # ==================== Logging Stack (Optional) ====================
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: elasticsearch
    environment:
      - node.name=elasticsearch
      - cluster.name=face-microservice-cluster
      - discovery.type=single-node
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
      - xpack.security.enabled=false
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    networks:
      - face
    restart: unless-stopped

  fluent-bit:
    image: fluent/fluent-bit:3.1.9
    container_name: fluent-bit
    profiles:
      - logging
    ports:
      - "24224:24224"
      - "2020:2020"
    volumes:
      - ./logging/fluent-bit-simple.conf:/fluent-bit/etc/fluent-bit.conf:ro
      - ./logging/parsers.conf:/fluent-bit/etc/parsers.conf:ro
    networks:
      - face
    depends_on:
      - elasticsearch
    restart: unless-stopped

  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.0
    container_name: kibana
    profiles:
      - logging
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - SERVER_NAME=kibana
      - XPACK_SECURITY_ENABLED=false
      - XPACK_ENCRYPTEDSAVEDOBJECTS_ENCRYPTIONKEY=a7a6311933d3503b89bc2dbc36572c4a
    networks:
      - face
    depends_on:
      - elasticsearch
    restart: unless-stopped

networks:
  face:
    driver: bridge

volumes:
  mysql:
  redis:
  minio-data:
  vault-data:
  elasticsearch-data:
  rabbitmq-data:
  face-model-cache:
