# Dev-specific nginx vhost overrides
# This file is copied to the server and can contain env-specific tuning
# The main default.conf is generated from the nginx.conf.j2 template

# Example: additional rate limiting for dev
# limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
