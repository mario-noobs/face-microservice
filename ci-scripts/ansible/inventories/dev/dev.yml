---
all:
  children:
    app_servers:
      hosts:
        dev-server-1:
          ansible_host: "mario-server.tail70f71b.ts.net"
          ansible_user: "{{ deploy_user }}"
          # Reached via Tailscale — CI brings up a tailnet node tagged tag:ci;
          # ACL allows tag:ci -> tag:dev-server:22. Local `make deploy-dev`
          # works too as long as your laptop is on the same tailnet.
