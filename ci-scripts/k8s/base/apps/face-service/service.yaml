apiVersion: v1
kind: Service
metadata:
  name: face-recognition-service
  namespace: face-app
  labels:
    app: face-recognition-service
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: face-recognition-service
